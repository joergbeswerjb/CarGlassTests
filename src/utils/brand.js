// ─── Бренд GlassGo — палитра, логотип, стилевые токены ──────────────────────
// Используется во всех тестах платформы (techник, OD, и будущие роли).

export const B = {
  // ─── Primary blue (логотип GlassGo) ─────────────────────────
  primary:       '#0F3876',   // основной — кнопки, акценты, активные состояния
  primaryDark:   '#082458',   // hover, нажатое состояние

  // ─── Поверхности и фоны ─────────────────────────────────────
  white:         '#FFFFFF',
  light:         '#F4F6F9',   // основной фон страниц
  dark:          '#1A202C',   // тёмные шапки, плашки
  border:        '#E0E4EB',   // тонкие границы

  // ─── Текст ──────────────────────────────────────────────────
  text:          '#212529',   // основной текст
  muted:         '#6C757D',   // вспомогательный

  // ─── Семантические (не брендовые, отвечают за состояние) ────
  green:         '#1A7A3C',   // ✓ заполнено, успех
  greenBg:       '#E8F5ED',
  amber:         '#BA7517',   // предупреждение, счётчик символов
  red:           '#C92020',   // ошибки валидации, critical_red
  redBg:         '#FCEBEB',

  // Совместимость с техником (старый CarGlass-стиль постепенно уходит)
  yellow:        '#FFD700',   // legacy

  // ─── Путь к логотипу (в public/) ────────────────────────────
  LOGO_PATH:     '/glassgo-logo.png',
}

// ─── Стилевые токены для асимметричной плашки «Go» ──────────────────────────
// Скруглены: правый-верхний и левый-нижний. Острые: левый-верхний, правый-нижний.
// Это создаёт диагональное движение и сохраняется как фирменный элемент.

export const SHAPE = {
  // Главные CTA-кнопки (Начать, Дальше, Завершить блок, Завершить тест)
  asymmetric: '12px 0 12px 0',
  // Маленький бейдж (версия, активный шаг)
  asymmetricSm: '8px 0 8px 0',
  // Большой акцент (титульная плашка)
  asymmetricLg: '16px 0 16px 0',
  // Обычные элементы — без асимметрии
  card: '8px',
  input: '6px',
}
